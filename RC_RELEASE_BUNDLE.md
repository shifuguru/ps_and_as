# RC Release Bundle — v1.0.61

**Scope freeze:** 2026-06-08  
**Baseline (production):** v1.0.60 @ `249eabb8298333e24f53578e54f7d45dffa5c7bd`  
**Target:** v1.0.61  
**Rule:** Every gameplay-affecting path is in exactly one bucket below.

---

## Summary

| Bucket | Tracked vs `249eabb` | Untracked working tree |
|--------|----------------------|-------------------------|
| **MUST SHIP** | 11 files (+ packaging on commit) | 4 gate scripts |
| **NICE TO SHIP** | 1 doc | 0 gameplay files |
| **POST-RC** | 3 files (Mission Control) | ~80+ paths |

**Commit strategy:** Stage **MUST SHIP only**. Do **not** stage `App.tsx`, `src/studio/`, `MissionControlScreen.tsx`, presence stack, or investigation markdown.

---

## MUST SHIP (v1.0.61)

Player-facing fixes validated (automated + live socket where noted).

### On Top grant-path + turn guards

| File | Rationale |
|------|-----------|
| `src/game/core.ts` | Recovers `tenRule.direction` from trick after sync strip; `isOnTopEligiblePile` grants On Top when direction recoverable; `resolveEffectiveTenRule` at grant sites |
| `src/screens/GameScreen.tsx` | `runOnTopActive` uses `currentPlayerIndex`; `humanRunOnTopTurn` pass/play guards; removes stale `useServerOpener` post-trade fallback |
| `server/index.js` | `runOnTopTurn` bypass for "Not your turn"; realigns `currentPlayerIndex` to on-top owner |

**Validated:** `npx tsx ./scripts/test-core.ts` (On Top + direction recovery); `npx tsx ./scripts/trace-on-top-pass-block.mjs` (OT-1/OT-2 proxy).

### Post-trade opener (3♣ holder)

| File | Rationale |
|------|-----------|
| `src/utils/tableSeats.ts` | `resolveOpenerAfterRoleTrades` — 3♣ only post-trade; dead-hand branch preserved inside `resolveLeadPlayerIndexAfterTrades` |
| `src/game/roundPrep.ts` | `reconcilePostTradeOpeningIndex`, `openingLeadNotYetTaken` |
| `src/game/roundTransitionDiagnostics.ts` | `logPostTradeOpenerReconciled` |
| `server/index.js` | `syncOpeningPlayerAfterTrades` before `broadcastGameState`; `resolveOpenerAfterRoleTrades`; instant-complete path in `startNextRound` |
| `src/screens/GameScreen.tsx` | `reconcileSyncedOpeningPlayer` in `applyServerSync` |

**Validated:** `test-core` production repro + dead-hand post-trade tests; `node scripts/test-post-trade-opener.mjs`; **live** `node scripts/live-post-trade-opener-verify.mjs` (PASS).

### Dead Hand opener protection

| File | Rationale |
|------|-----------|
| `src/utils/tableSeats.ts` | Unchanged dead-hand branch in `resolveLeadPlayerIndexAfterTrades` — regression covered in `test-core` post-trade block |

**Validated:** Unit tests only (no live 2p+dead-hand socket run this sprint).

### Ceremony / round-transition (partial — delta vs production)

| File | Rationale |
|------|-----------|
| `src/screens/GameScreen.tsx` | `finalizeCeremonyRound` uses `baseState.lastRoundOrder` for dealer context; post-trade opener reconcile prevents stale sync overwriting opener |

**Note:** Production **1.0.60** already ships "Fresh round & online dealing" (What's New). This RC adds **opener sync + reconcile**, not the full `shouldFinalizeInstantTradesComplete` helper referenced in older docs (symbol **absent** from code).

**Validated:** `test-core` fresh-round tests; release gate `quick-private-2h`; R-1 proxy `test-connected-round-end-order.mjs`.

### D-010 — BOTOPN hide

| File | Rationale |
|------|-----------|
| `src/utils/roomCode.ts` | `BOT_PUBLIC_ROOM_CODE`, `isBotPublicRoomCode()` |
| `src/screens/FindGame.tsx` | Filter bot rooms from list; empty state **"No Public Games Available"**; block join-by-code `BOTOPN` |

**Validated:** Code audit (D10-1); not browser-smoked this sprint.

### Gate / regression (SHOULD commit with RC)

| File | Rationale |
|------|-----------|
| `scripts/test-core.ts` | On Top, post-trade opener, dead-hand post-trade, client reconcile |
| `scripts/test-release-gate.mjs` | `SKIP_BOTOPN` / `RC_SCOPE` waiver for D-010 |
| `scripts/test-post-trade-opener.mjs` | Server sync-before-broadcast ordering |
| `scripts/test-connected-round-end-order.mjs` | Rankings-after-last-hand overlay order (R-1 proxy) |
| `scripts/trace-on-top-pass-block.mjs` | On Top pass-block trace (OT-1 proxy) |
| `scripts/live-post-trade-opener-verify.mjs` | Live 3p post-trade opener repro (O-1) |

### Release packaging (on commit — not yet staged)

| File | Action |
|------|--------|
| `package.json` | Set `"version": "1.0.61"` |
| `src/screens/updateLogContent.ts` | New top entry (see `v1.0.61_CHANGELOG_DRAFT.md`) |
| `src/config/buildCodenames.ts` | Add `"1.0.61": "<codename>"` via bump workflow or manual |
| `version.json` | Regenerated by deploy / `build:web` — do not commit stale root copy |

---

## NICE TO SHIP

Low-risk polish **not required** for RC. **None** in tracked diff vs `249eabb` qualify as separate gameplay polish.

| File | Rationale |
|------|-----------|
| `RELEASE_GATE.md` | Gate doc tweak — optional with RC |

**Untracked polish (exclude from v1.0.61 commit):**

- `src/components/GameTable.tsx` — turn-hint pill width  
- `src/components/TrickWinShout.tsx` — `autoDismiss`  
- `src/utils/playerDisplay.ts` — pill truncation  
- `src/utils/playAnimationTiming.ts` — flight timing constant  

---

## POST-RC

### Tracked vs production — exclude from RC commit

| File | Rationale |
|------|-----------|
| `App.tsx` | Mission Control web route gate |
| `src/components/AppErrorBoundary.tsx` | Mission Control crash surface |
| `src/studio/loadStudioData.ts` | Mission Control data loader (new) |

### Untracked — do not stage

| Category | Paths |
|----------|-------|
| **Mission Control / Studio** | `studio/**`, `public/studio/**`, `src/studio/**`, `src/screens/MissionControlScreen.tsx`, `scripts/studio/**`, `.cursor/rules/studio-orchestrator.mdc` |
| **Presence V2 / ring** | `src/presence/**`, `src/components/PresenceRing*.tsx`, `LegacyTurnRing.tsx`, `scripts/presence-ring-*`, `scripts/test-presence-ring.ts` |
| **Seat chat** | `src/components/GameSeatChatButton.tsx`, `src/utils/seatChat.ts` |
| **BOTOPN lifecycle** | Deferred D-010 — server `botHostedRooms.js` tuning unchanged in RC |
| **IPP / identity** | Not in tree slice |
| **Investigations / reports** | `*_INVESTIGATION.md`, `RC_FINAL_READINESS_REPORT.md`, `V1_0_61_*.md`, `LIVE_*.md`, `gate-run-*.txt`, `test-results/**` |
| **Run-highlight / turn-ownership experiments** | `scripts/turn-ownership/**`, `scripts/human-interaction/**` |
| **Stale local build** | `web-build/**` (regenerate at release) |

---

## Exact commit list (recommended)

```text
src/game/core.ts
src/game/roundPrep.ts
src/game/roundTransitionDiagnostics.ts
src/utils/tableSeats.ts
src/utils/roomCode.ts
src/screens/GameScreen.tsx
src/screens/FindGame.tsx
server/index.js
scripts/test-core.ts
scripts/test-release-gate.mjs
scripts/test-post-trade-opener.mjs
scripts/test-connected-round-end-order.mjs
scripts/trace-on-top-pass-block.mjs
scripts/live-post-trade-opener-verify.mjs
package.json
src/screens/updateLogContent.ts
src/config/buildCodenames.ts
```

**14 gameplay + 7 verification + 3 packaging = 24 files**

---

## Out of scope confirmation

- No new features  
- No BOTOPN reactivation  
- No Mission Control expansion  
- No IPP  
- No presence ring in RC commit  
