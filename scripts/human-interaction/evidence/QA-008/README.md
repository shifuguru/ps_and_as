# QA-008 evidence

Attach per-session packages here:

- Screen recording or frame capture
- Console export (same session)
- Completed checklist excerpt or copy (`../checklist.md`)

If logs show duplicate `[FLIGHT] CREATED` but video shows one smooth flight, classify as architecture duplication — do not implement.
